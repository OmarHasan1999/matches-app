services:
  frontend:
    build: ./front
    ports:
      - "5173:5173"
    volumes:
      - ./front:/app
      - /app/node_modules
    depends_on:
      - backend
    command: npm run dev

  backend:
    build: ./server
    ports:
      - "3001:7860"
    volumes:
      - ./server:/app
      - /app/node_modules
    environment:
      - PORT=7860
    command: npm run dev